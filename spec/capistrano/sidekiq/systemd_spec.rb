RSpec.describe Capistrano::Sidekiq::Systemd do
  it "has a version number" do
    expect(Capistrano::Sidekiq::Systemd::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
